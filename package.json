{
  "name": "jules-server",
  "version": "0.0.0",
  "description": "A library for nodejs to resolute DRY code.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./build-pre && tsc --outDir ./build && node ./build-post",
    "pub": "npm run build && cd ./build && npm publish && cd .."
  },
  "author": "Jules Patry",
  "url": "https://github.com/JulesPatry/jules-server",
  "email": "julespatry@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.4",
    "typescript": "^4.4.4"
  }
}
